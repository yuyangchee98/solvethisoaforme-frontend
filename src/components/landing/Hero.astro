---
---

<section class="relative overflow-hidden">
  <!-- Subtle background gradient -->
  <div class="absolute inset-0 bg-gradient-to-b from-amber-50/60 via-transparent to-transparent pointer-events-none"></div>

  <div class="relative max-w-6xl mx-auto px-6 pt-16 pb-20 md:pt-24 md:pb-28">
    <div class="max-w-3xl mx-auto text-center">
      <!-- Badge -->
      <div class="inline-flex items-center gap-2 px-3 py-1.5 rounded-full bg-amber-100/80 border border-amber-200/60 mb-8">
        <span class="w-1.5 h-1.5 rounded-full bg-amber-500"></span>
        <span class="text-xs font-medium text-amber-800 tracking-wide">AI-Powered Patent Prosecution</span>
      </div>

      <!-- Headline -->
      <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold text-stone-900 tracking-tight leading-[1.1] mb-6">
        Respond to Office Actions
        <span class="text-amber-600"> in minutes,</span>
        <br class="hidden sm:block" />
        not days
      </h1>

      <!-- Subtitle -->
      <p class="text-lg md:text-xl text-stone-500 leading-relaxed max-w-2xl mx-auto mb-10">
        Upload your Office Action and let AI analyze rejections, evaluate prior art, and draft response strategies — so you can focus on the prosecution decisions that matter.
      </p>

      <!-- CTAs -->
      <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
        <a
          href="/agent"
          class="inline-flex items-center gap-2 px-6 py-3 text-base font-medium text-white bg-amber-600 hover:bg-amber-700 rounded-lg transition-colors shadow-sm"
        >
          Start Analyzing Free
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
        </a>
        <a
          href="#how-it-works"
          class="inline-flex items-center gap-2 px-6 py-3 text-base font-medium text-stone-700 bg-white hover:bg-stone-50 border border-stone-200 rounded-lg transition-colors"
        >
          See How It Works
        </a>
      </div>
    </div>

    <!-- Product mockup placeholder -->
    <div class="mt-16 md:mt-20 max-w-4xl mx-auto">
      <div class="relative rounded-xl border border-stone-200/80 bg-white shadow-lg overflow-hidden">
        <!-- Fake browser chrome -->
        <div class="flex items-center gap-2 px-4 py-3 bg-stone-50 border-b border-stone-100">
          <div class="flex gap-1.5">
            <div class="w-3 h-3 rounded-full bg-stone-200"></div>
            <div class="w-3 h-3 rounded-full bg-stone-200"></div>
            <div class="w-3 h-3 rounded-full bg-stone-200"></div>
          </div>
          <div class="flex-1 mx-8">
            <div class="h-6 bg-stone-100 rounded-md max-w-xs mx-auto"></div>
          </div>
        </div>
        <!-- Content area with gradient placeholder -->
        <div class="aspect-[16/9] bg-gradient-to-br from-amber-50 via-stone-50 to-amber-50/50 flex items-center justify-center p-8">
          <div class="text-center space-y-4 max-w-md">
            <div class="inline-flex items-center justify-center w-16 h-16 rounded-2xl bg-amber-100 mx-auto">
              <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" class="text-amber-600"><path d="M14.5 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7.5L14.5 2z"/><polyline points="14 2 14 8 20 8"/><path d="M12 18v-6"/><path d="m9 15 3-3 3 3"/></svg>
            </div>
            <p class="text-stone-400 text-sm font-medium">Upload an Office Action to get started</p>
            <div class="flex justify-center gap-3">
              <div class="h-8 w-20 rounded-md bg-amber-200/40"></div>
              <div class="h-8 w-24 rounded-md bg-stone-200/40"></div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
